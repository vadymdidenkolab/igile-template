# A new project

This repository is an igile vault: a task tracker and a knowledge base made of Markdown files.

**Read [AGENTS.md](AGENTS.md) before changing anything in it.** That file is the whole of what
you need: where a task lives, what its frontmatter means, why a relationship has to be a
wikilink, which properties move together, and what a commit here is.

It is one file rather than two because the rules are the same whichever agent is reading them,
and two copies of a rule is one copy that goes stale.
