# Regenerated by igile. Projects are named explicitly; run
# "igile project add" to add one and rewrite these filters.
filters:
  and:
    - or:
      - file.inFolder("PROJ")
    - 'note.status_category != "done"'
formulas:
  stage: 'if(note.status == "Backlog", "1. Backlog", if(note.status == "Ready", "2. Ready", if(note.status == "In progress", "3. In progress", if(note.status == "In review", "4. In review", if(note.status == "Done", "5. Done", if(note.status == "Dropped", "6. Dropped", ""))))))'
properties:
  formula.stage:
    displayName: Stage
  note.title:
    displayName: Title
  note.assignee:
    displayName: Assignee
  note.priority:
    displayName: Priority
  note.tags:
    displayName: Tags
views:
  - type: cards
    name: Board
    groupBy:
      property: formula.stage
      direction: ASC
    order:
      - note.title
      - note.assignee
      - note.priority
      - note.labels
      - note.tags
